import { PortalRowDirective } from './portal-row.directive';

describe('PortalRowDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalRowDirective();
    expect(directive).toBeTruthy();
  });
});
